// place files you want to import through the `$lib` alias in this folder.

export type User = {
	username: string
}

export type Post = {
	title: string
	content: string
	createdAt: string
	id: string
	username: string
}