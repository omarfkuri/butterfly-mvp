spring:
  application:
    name: api
    
  datasource:
    url: jdbc:h2:file:/data/socialdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  jpa:
    hibernate:
      # ddl-auto: create-drop
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true
